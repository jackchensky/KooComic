# KOOBONE × KOReader 插件项目状态

> 本文件是项目从 v0.1 起的持续状态记录。今后的开发结果、遗留问题和下一步统一维护在这里。
>
> 最后更新：2026-08-21

## 1. 项目目标

开发一个原生 KOReader 插件，让 Kindle 用户能够登录自己的 KOOBONE 账号、浏览个人漫画书库、下载 KOOBONE 已生成的 EPUB，并直接使用 KOReader 阅读。

项目不绕过 KOOBONE 权限，不破解 Kxx.moe 的 Lv3 下载限制，也不保存或分发他人的漫画资源。插件只访问当前登录账号原本有权访问的书库和文件。

## 2. 当前仓库状态

- Git：已在项目根目录初始化，默认分支为 `main`，并已建立 v0.1 首次基线提交。
- 当前插件版本：v0.4.0 开发测试版；v0.1 保留为已提交的可回退基线。
- 插件名称：`koo漫画`；这是连接 KOOBONE 个人漫画书库的非官方 KOReader 插件。
- 插件源码目录：`koocomic.koplugin/`。
- v0.1 发布归档：`koobone-koreader-plugin-v0.1.zip`，作为本地历史产物保留，不纳入 Git。
- 原始研究记录：`koobone-koreader-codex-context/KOOBONE_KOReader_Project_Handoff.md`。
- 原始抓包截图：保留在本地交接资料目录，但因可能含账号或会话信息，已通过 `.gitignore` 排除。
- 后续状态记录：以本文件为准；原始交接文档只作为 v0.1 调研档案，不继续追加。

当前插件结构：

```text
koocomic.koplugin/
├── _meta.lua
├── LICENSE
├── NOTICE
├── main.lua
├── koobone/
│   ├── api.lua
│   ├── auth.lua
│   ├── cover_cache.lua
│   ├── downloader.lua
│   ├── library.lua
│   ├── plugin_version.lua
│   ├── settings.lua
│   ├── storage.lua
│   ├── updater.lua
│   ├── util.lua
│   └── ui/
│       ├── account.lua
│       ├── book_detail.lua
│       ├── bookshelf.lua
│       ├── bookshelf_grid.lua
│       ├── download_dialog.lua
│       ├── downloads.lua
│       ├── info_panel.lua
│       ├── login_dialog.lua
│       ├── metrics.lua
│       ├── settings.lua
│       └── updater.lua
└── README.md
```

项目采用 `AGPL-3.0-only`，完整许可证位于仓库根目录和发布插件目录的 `LICENSE`；`NOTICE` 明确说明项目与 KOOBONE、Bookof.hk 及其开发者不存在隶属、赞助或官方合作关系。

## 3. v0.1 已打通的核心闭环

当前 v0.1 已能在 KOReader 中显示 KOOBONE 入口，并完成以下基本流程：

```text
打开 KOOBONE 插件
  ↓
输入 KOOBONE 邮箱和密码
  ↓
获取 VLIBSID 并提交登录请求
  ↓
取得 KBSKEY 会话 Cookie
  ↓
调用用户信息接口取得 uin / 昵称
  ↓
调用书库接口取得漫画列表和临时签名下载地址
  ↓
选择漫画并下载完整 EPUB
  ↓
保存到 Kindle 本地 KOOBONE 目录
  ↓
调用 KOReader 打开 EPUB
```

已实际确认的用户侧结果：

- KOReader 能看到 KOOBONE 书库中的漫画。
- 能选择漫画并下载 EPUB。
- 下载完成后可以交给 KOReader 打开。
- 当前下载期间看不到实时进度，界面会缺少反馈。
- 第二次登录仍需重新输入用户名和密码。

## 4. v0.1 网络流程

### 4.1 初始化会话

登录前访问 KOOBONE 动态登录入口，取得或复用 `VLIBSID`。2026-08-20 的匿名请求验证结果为：`/login.htm` 只返回静态登录页且不下发 Cookie；`/login.php?goto=web.htm` 返回 `VLIBSID`；站点根路径的 302 响应也会返回 `VLIBSID`。v0.2.1 因此优先使用 `/login.php` 初始化会话，并以根路径作为兼容回退。

### 4.2 提交登录

```text
POST /login_do.php
Content-Type: multipart/form-data

字段：
- email
- passwd
- keepalive=1
```

登录成功后，服务端通过 `Set-Cookie` 返回 `KBSKEY`。后续请求需要同时携带 `VLIBSID` 和 `KBSKEY`。

### 4.3 获取用户信息

```text
GET /uinfo.php?v=web&ver=7
Cookie: VLIBSID + KBSKEY
```

响应中包含当前账号的 `uin`、昵称等信息。`uin` 用于请求当前用户的书库。

### 4.4 获取个人书库

```text
GET /vol_list.php?u=<uin>&by=time&limit=28
Cookie: VLIBSID + KBSKEY
```

已观察到的主要书籍字段：

- `vol_name`：书名。
- `vol_author`：作者。
- `file_md5`：文件标识。
- `file_size`：文件大小。
- `cover_url` / `cover_small`：封面地址。
- `last_readpage` / `count_page`：KOOBONE 侧阅读位置和页数。
- `file_url`：带临时签名的 EPUB 下载地址。

当前 v0.1 使用固定 `limit=28`，尚未完成完整分页。

### 4.5 下载 EPUB

书库接口直接返回带签名的 `file_url`。访问该地址会返回标准 `application/epub+zip` 文件，因此插件不需要逐页抓取漫画图片，也不需要自己计算下载签名。

`file_url` 可能带有效期，不能作为长期缓存。后续实现应在用户开始下载前刷新书库数据，取得最新地址后立即下载。

### 4.6 打开本地文件

下载完成后，v0.1 将 EPUB 保存到 KOReader 可访问的 `KOOBONE` 目录，并调用 KOReader 阅读界面打开文件。

## 5. v0.1 代码实现概况

当前全部逻辑集中在 `main.lua`，主要职责包括：

- 加载和保存插件设置。
- 注册 KOReader 工具菜单入口。
- 检查登录状态和网络状态。
- 构造公共请求 Header 与 Cookie。
- 执行 HTTP 请求。
- 取得初始 `VLIBSID`。
- 构造登录用 multipart 请求体。
- 提交登录并解析 `KBSKEY`。
- 获取用户资料和漫画书库。
- 显示登录框与简单书籍列表。
- 计算本地下载目录和安全文件名。
- 下载 EPUB 并打开。

v0.1 的登录设计原则是只保存会话 Cookie，不保存密码。当前界面没有“记住密码”和“退出账号”。

## 6. v0.1 调研过程与关键结论

### 6.1 为什么从 KOOBONE 入手

最初研究过 Kxx.moe 的下载流程，但 KOOBONE 已经能为用户账号中的漫画提供完整 EPUB。直接使用 KOOBONE 的个人书库接口，技术链路更短，也不需要处理 Lv3 下载逻辑。

### 6.2 确认实际下载对象

通过浏览器 Network 记录确认，用户点击漫画后，KOOBONE 网页会先请求完整 EPUB，再在网页阅读器中显示。因此 KOReader 插件可以直接下载 EPUB，而无需复刻网页阅读器。

### 6.3 排除错误方向

早期曾研究 `vol_act.php`，之后确认它更可能用于阅读行为或状态上报，不是取得 EPUB 地址的接口。真正的下载地址由 `vol_list.php` 的 `file_url` 字段提供。

### 6.4 JavaScript 调用链结论

网页端的下载函数最终接收后端已经生成的 `file_url`。插件不需要从 JavaScript 中重做签名算法，也不需要长期保存带 `sign` 的地址。

### 6.5 登录和 Cookie 结论

- 登录页面本身不是登录处理接口。
- 真正的登录提交发生在 `login_do.php`。
- `VLIBSID` 用于初始会话。
- 登录成功后取得 `KBSKEY`。
- 后续用户信息、书库等请求依赖两项会话 Cookie。
- Cookie 失效返回形式和具体生命周期仍需真机测试。

## 7. 已确认不需要做的工作

- 不需要解析 Kxx.moe 的 Lv3 EPUB 下载流程。
- 不需要逐页抓取漫画图片。
- 不需要复刻 KOOBONE 网页阅读器。
- 不需要从 `vol_act.php` 获取 EPUB 地址。
- 不需要由插件计算 `file_url` 的签名。
- 当前阶段不做 KOOBONE 与 KOReader 的阅读进度同步。

## 8. 当前技术风险和未解决问题

### 8.1 HTTPS 与 Kindle 网络兼容

KOOBONE 页面和下载地址均为 HTTPS。v0.1 仍需验证当前 `socket.http` 使用方式在不同 KOReader/Kindle 版本上的兼容性，并评估切换到 KOReader 的 `ssl.https`、`socketutil` 等网络能力。

### 8.2 Cookie 解析

不同 Lua HTTP 实现可能把 `Set-Cookie` 返回为字符串、表，或使用不同大小写的 Header 名。必须可靠解析多个 Cookie，且日志中不能输出 Cookie 值。

### 8.3 会话生命周期

仍需确认：

- `VLIBSID` 和 `KBSKEY` 的有效期。
- `keepalive=1` 的具体效果。
- `uinfo.php` 和 `vol_list.php` 在登录过期时的响应。
- 登录过期后自动重试和重新登录的边界。

### 8.4 大文件下载

漫画 EPUB 常为几十到数百 MB。v0.1 缺少：

- 实时字节进度。
- 下载速度和预计剩余时间。
- `.part` 临时文件。
- 下载取消。
- 失败后续传。
- 弱网重试。
- 后台下载管理。

### 8.5 UI 阻塞

当前网络和下载过程可能阻塞界面。应使用 KOReader 的进度对话框、进度回调和必要的独立任务机制，控制墨水屏刷新频率。

### 8.6 书库分页

当前接口限制为 28 项。需要验证 offset/page 参数并支持继续加载、刷新和错误重试。

### 8.7 阅读页码映射

KOOBONE 的 `last_readpage` 不一定与 KOReader 的 EPUB 页码完全一致。在验证映射前只显示远端阅读信息，不进行双向同步。

## 9. v0.2 目标与界面规划

v0.2 优先改善用户真正会遇到的问题：下载反馈、登录体验、账号管理、书架易用性和版本更新。

### 9.1 推荐代码结构

```text
koobone.koplugin/
├── _meta.lua
├── version.lua
├── main.lua
├── api.lua
├── auth.lua
├── settings.lua
├── library.lua
├── storage.lua
├── downloader.lua
├── updater.lua
├── ui/
│   ├── login_dialog.lua
│   ├── bookshelf.lua
│   ├── book_detail.lua
│   ├── download_dialog.lua
│   ├── download_manager.lua
│   ├── account.lua
│   └── update_dialog.lua
└── README.md
```

职责原则：

- `api.lua`：统一 HTTPS、Header、Cookie、JSON、超时和错误映射。
- `auth.lua`：登录、会话验证、自动恢复、退出账号。
- `library.lua`：书库数据、分页、刷新和临时下载地址更新。
- `storage.lua`：目录、文件名、已下载判断、封面缓存。
- `downloader.lua`：流式下载、进度、`.part`、取消和续传。
- `updater.lua`：版本清单、安装包校验、备份、安装和回滚。
- `ui/`：各页面和对话框，不直接承担网络细节。

### 9.2 登录与账号

登录页计划增加：

- 邮箱。
- 密码。
- “记住密码”复选项，默认关闭。
- 明确说明开启后密码只保存在本机插件设置中，KOReader 没有可依赖的系统钥匙串保护。

登录恢复顺序：

```text
启动插件
  ↓
优先验证已有 Cookie
  ↓
Cookie 有效：直接进入书架
  ↓
Cookie 失效且用户选择记住密码：尝试重新登录一次
  ↓
仍失败：显示登录页，不循环提交密码
```

账号页计划显示昵称、账号状态、会话状态，并提供：

- 重新登录。
- 清除已保存密码。
- 退出账号。

退出账号时清除 Cookie 和已保存密码，但默认保留已经下载的 EPUB、封面缓存和阅读记录。

### 9.3 书架

- 已登录时直接进入书架，不重复显示登录页。
- 竖屏优先使用三列封面网格，横屏可增加列数；低性能设备可回退到列表。
- 封面显示下载状态和阅读进度徽标。
- 支持刷新、继续加载、下载管理、账号和设置入口。
- 封面缓存以稳定文件标识为 key，避免重复下载和大面积刷新。

### 9.4 书籍详情

显示封面、书名、作者、文件大小、KOOBONE 阅读进度和本地下载状态。主要操作为：

- 下载。
- 继续下载。
- 打开本地文件。
- 重新下载。

### 9.5 下载进度与下载管理

前台下载对话框至少显示：

- 当前阶段。
- 百分比。
- 已下载字节 / 总字节。
- 当前速度。
- 预计剩余时间。
- 取消按钮。

下载流程：

```text
刷新书库并取得最新 file_url
  ↓
流式写入 <文件名>.epub.part
  ↓
按节流频率刷新进度
  ↓
验证响应和文件大小
  ↓
原子重命名为 <文件名>.epub
  ↓
更新下载状态并允许打开
```

真正的后台下载、任务队列和稳定取消需要独立 worker，可在前台进度稳定后继续实现。

### 9.6 版本与在线更新

设置页增加“版本与更新”：

- 当前版本。
- 手动检查更新。
- 每天自动检查，默认关闭。
- 自动更新提醒状态。
- 更新通道，首版只提供正式版。
- 上次检查时间。
- 更新安全说明。

推荐使用固定 HTTPS 地址提供 `update.json`，清单包含：

- 新版本号。
- 更新通道和发布日期。
- 安装包 URL、文件大小和 SHA-256。
- 最低 KOReader 版本。
- 更新摘要和更新说明。

安装过程：

```text
检查 update.json
  ↓
比较语义化版本号
  ↓
用户确认下载
  ↓
显示更新包下载进度
  ↓
验证大小和 SHA-256
  ↓
安全解压到暂存目录
  ↓
检查唯一插件根目录、main.lua、_meta.lua 和版本号
  ↓
备份当前 koobone.koplugin
  ↓
切换新版本，失败则立即回滚
  ↓
提示重启 KOReader
  ↓
新版本启动确认后再清理备份
```

自动检查只读取版本清单，不自动安装。漫画正在下载时不得开始安装更新。初期使用 GitHub HTTPS 和 SHA-256；后续可增加签名清单和不会被升级覆盖的最小启动引导器，实现启动失败自动回滚。

#### 新版本自动提醒规则

启用每天自动检查后，如检测到比当前安装版本更新的正式版，显示确认弹窗：

```text
发现新版本

KOOBONE v0.3.0 已发布
当前版本：v0.2.0

更新摘要和安装包大小

[稍后更新]    [立即更新]
```

交互规则：

1. `立即更新`：进入更新说明和下载安装流程，不增加稍后次数。
2. `稍后更新`：关闭弹窗并将累计稍后次数加一。
3. 自动弹窗按设备本地日期限制为每天最多一次；重启 KOReader 或重复打开插件不能在同一天再次触发。
4. 用户累计三次明确选择 `稍后更新` 后，设置 `auto_update_prompt_disabled=true`，以后任何新版本都不再自动弹窗。
5. 三次限制是设备级持久状态，不因线上目标版本变化、KOReader 重启或插件重新进入而自动清零。
6. 用户点击返回键或关闭按钮时，按一次 `稍后更新` 处理，避免通过关闭方式每天重复提醒。
7. 网络错误、清单解析失败、安装失败或下载失败不计入稍后次数。
8. 手动进入设置执行“检查更新”不受每天一次和三次限制，也不会增加稍后次数。
9. 自动提醒停止后，设置页显示“更新提醒：已停止（已稍后 3 次）”，但“检查更新”按钮始终保留。
10. 用户若在设置中主动重新开启“自动更新提醒”，需要明确确认；确认后清除三次计数并从零重新计算。

需要持久保存但不得包含敏感信息的状态字段：

```text
auto_update_check_enabled
last_auto_update_check_at
last_update_prompt_date
update_prompt_later_count
auto_update_prompt_disabled
last_seen_update_version
```

自动检查与自动弹窗应分开控制：可以在后台完成一次轻量版本检查，但只有满足“发现新版本、当天未弹过、累计稍后少于三次、提醒未被禁用”四个条件时才显示弹窗。手动检查始终由用户主动触发，不读取自动弹窗限制。

### 9.7 v0.2.0 当前实现状态

2026-08-20 已完成第一轮 v0.2.0 编码，当前为需要 Kindle 真机验证的开发测试版。

已编码：

- 将 v0.1 单文件实现拆分为带 `koobone.*` 命名空间的网络、认证、设置、书库、存储、下载、更新和 UI 模块。
- 自动迁移 v0.1 的邮箱、Cookie、用户编号和昵称，不保存旧版本不存在的密码。
- 优先验证现有会话；会话失效且用户主动开启记住密码时，只自动重新登录一次。
- 登录页增加“记住密码（密码将保存在本机设置中）”，默认关闭。
- 增加账号状态、重新登录、清除保存密码和退出账号；退出账号保留已下载文件。
- 使用 KOReader `Menu` 实现分页列表式书架，增加书籍详情、本地状态、刷新、下载管理和设置入口。
- EPUB 流式写入 `.part`，显示百分比、字节数、平均速度和预计剩余时间。
- 下载失败或取消时保留 `.part`，成功后使用 `os.rename` 切换为正式 EPUB。
- 增加下载记录与已下载文件打开入口。
- 增加固定 HTTPS `update.json` 检查、语义化版本比较、更新提示和手动检查入口。
- 实现每天最多一次自动提醒、累计三次“稍后更新”后停止提醒，以及设置中主动恢复提醒。
- 实现更新包大小限制、流式 SHA-256、安全压缩包路径检查、暂存解压、旧插件备份、切换失败回滚和重启生效。
- 增加可重复发布脚本与 GitHub Actions：按标签打包 Release ZIP、生成 SHA-256/`update.json`、创建 GitHub Release，并将固定清单发布到 GitHub Pages。
- 生成本地测试包 `koobone-koreader-plugin-v0.2.0.zip`，ZIP 完整性检查通过。

当前有意保留的限制：

- GitHub 仓库尚未确定，因此 `manifest_url` 和 `release_prefix` 为空；在线检查会安全提示尚未配置，不访问占位域名。
- 书架目前是墨水屏友好的分页列表和详情页，封面网格及封面缓存尚未实现。
- 当前书库仍请求前 28 项，服务器分页参数尚未真机验证。
- `.part` 会保留，但再次下载目前从头写入；Range 断点续传尚未启用，避免服务器忽略 Range 时拼接出损坏文件。
- 下载仍是前台网络任务；真正后台 worker 和下载队列尚未实现。
- 取消逻辑会在下一个网络数据块到达时终止写入，但不同 Kindle/KOReader 版本是否能在同步网络请求期间及时派发触摸事件仍需真机确认。
- 没有加入安装统计、设备型号上报或其他遥测。

### 9.8 v0.2.1 登录兼容修复

第一轮 Kindle 真机登录显示“登录页面没有返回 VLIBSID”。定位结果不是账号密码错误，而是 v0.2.0 请求了静态入口 `/login.htm?goto=web.htm`，并错误地要求该响应必须下发 `VLIBSID`；当前站点实际从 `/login.php?goto=web.htm` 或站点根路径下发初始会话 Cookie。

v0.2.1 已完成：

- 初始化会话改为优先请求 `/login.php?goto=web.htm`。
- 若动态登录入口未返回 `VLIBSID`，回退请求站点根路径，并接受其携带 Cookie 的 302 响应。
- 登录成功响应若轮换 `VLIBSID`，与 `KBSKEY` 一并保存。
- 新增认证 API 回归测试，覆盖动态入口、根路径回退、多值 `Set-Cookie` 和会话 Cookie 轮换。
- 匿名线上核对过程只记录 Cookie 名称，不记录任何 Cookie 值，也未发送账号密码。
- 已生成可安装测试包 `koobone-koreader-plugin-v0.2.1.zip`，压缩包完整性和顶层 `koobone.koplugin/` 目录结构检查通过。

### 9.9 v0.2.2 书架刷新崩溃修复

第二轮 Kindle 真机测试确认登录已能继续，但刷新书架会退出 KOReader。`crash.log` 堆栈定位到 `storage.lua` 的 `.part` 文件大小检查，不是内存不足：当临时文件不存在时，KOReader 的 `lfs.attributes(path, "size")` 返回 `nil, error_string`；旧代码将多返回值直接传给 `tonumber`，导致错误字符串被误当作数字进制参数并抛出异常。

v0.2.2 已完成：

- 先单独接收 `lfs.attributes` 的第一个返回值，再传入 `tonumber`；不存在 `.part` 文件时稳定返回 0 字节。
- 修正在线更新包大小验证中的同类 Lua 多返回值隐患。
- 在书架网络刷新和菜单生成外层增加异常保护，后续兼容问题应显示错误信息，而不是退出 KOReader。
- 新增存储回归测试，覆盖 `.part` 不存在和正常返回大小两种情况。
- 新增书架回归测试，验证条目渲染异常会被界面捕获。
- 用户提供的日志截图未写入仓库；检查时未发现账号、Cookie 或签名 URL。
- 已生成 `koobone-koreader-plugin-v0.2.2.zip`，压缩包完整性和插件顶层目录结构检查通过。

### 9.10 v0.3.0 封面网格书架

用户真机确认 v0.2.2 仍显示 KOReader `Menu` 列表，与第 9.3 节和界面稿规划的三列封面书架不一致。此前将“分页列表可用”描述为按规划完成不准确；v0.3.0 开始补齐原规划界面。

v0.3.0 已编码：

- 使用 KOReader 原生全屏容器、触控区域和图片控件实现自定义书架，不使用网页界面。
- 竖屏固定三列、两行、每页六本；28 本书对应五页。横屏使用四列、两行。
- 顶部显示返回、KOOBONE 标题、昵称/数量、刷新和更多菜单。
- 第二行提供书名/作者搜索、最近加入/标题/阅读进度排序、全部/已下载/未下载/阅读中/未阅读筛选。
- 卡片显示真实缓存封面；尚未缓存或加载失败时显示带书名的墨水屏占位封面。
- 封面右上角显示已下载、未完成或 KOOBONE 阅读百分比角标。
- 页脚提供首页、上一页、页码、下一页和末页操作，并支持左右滑动及 Kindle 翻页键。
- 底部提供书架、下载、账号和设置导航，书架以反白状态标记当前页。
- 新增 HTTPS 封面缓存，按稳定书籍标识保存，不把 Cookie、密码或签名 EPUB 地址写入缓存。
- 当前页封面逐个获取；先显示可操作的占位书架，再渐进更新已取得的封面。
- 若旧 KOReader 缺少自定义网格所需组件或真机构建失败，保留原列表作为兼容回退并显示原因。
- 新增网格布局和封面缓存回归测试；竖屏 3×2、28 本五页、末页四本和筛选后页码收敛均有覆盖。
- 用户提供的界面截图未写入仓库，截图中未发现账号、Cookie 或签名 URL。
- 认证、网格、书架异常保护、封面缓存、插件加载、存储和更新策略七组测试通过。
- 已生成 `koobone-koreader-plugin-v0.3.0.zip`，压缩包完整性、顶层插件目录和新增网格/封面模块均检查通过。

### 9.11 v0.3.1 Kindle 下载网络兼容修复

v0.3.0 Kindle 真机下载显示“下载失败，临时文件已保留：network error”。该文字不是服务端 HTTP 状态，而是插件丢失底层连接错误后的通用回退信息。

定位与修复结果：

- v0.3.0 根据 URL 直接选择 `ssl.https.request`；但 KOReader 官方网络代码统一把 HTTPS URL 交给 `socket.http.request`，由 KOReader 的 LuaSocket 完成 TLS 调整。v0.1 也使用这一兼容路径，且已在同一设备上成功下载。
- v0.3.1 的所有网络请求恢复使用 KOReader 统一的 `socket.http.request` 入口，避免直接调用 LuaSec 绕过 Kindle/KOReader 的 HTTPS 与重定向兼容处理。
- 修正 LuaSocket 失败返回值处理：`nil, "timeout"`、`nil, "closed"` 等错误不再因提前执行 `tonumber` 而丢失。若真机仍失败，界面会显示可诊断的底层原因，而不是笼统的 `network error`。
- 保留现有 `.part` 安全策略；再次下载仍从头覆盖临时文件，不会把上一次残片拼接进 EPUB。
- 认证、网格、书架异常保护、封面缓存、插件加载、存储和更新策略七组回归测试通过。
- 已生成 `koobone-koreader-plugin-v0.3.1.zip`；ZIP 完整性、顶层 `koobone.koplugin/`、双版本号和敏感运行文件排除检查通过。
- 安装包 SHA-256：`6b2543be14b3c2b2322d0939ee6aed2f0d988a868c285746b8ffef920b3d01ea`。

### 9.12 v0.4.0 “koo漫画”更名与自适应界面

v0.4.0 将插件从直接使用第三方应用名的 `KOOBONE for KOReader` 更名为 `koo漫画`。KOOBONE 名称只用于说明所连接的兼容服务；关于页、README 和 `NOTICE` 均明确这是独立开发的非官方插件。

已完成：

- 插件发布目录改为 `koocomic.koplugin/`，KOReader 菜单名称改为“koo漫画”，版本号升级为 v0.4.0。
- 菜单排序提示从 `more_tools` 改为 `tools`，使插件入口与 MiuRead 等插件一样注册在 KOReader 工具一级菜单，而不是“更多工具”子菜单。
- 新设置使用 `settings/koocomic.lua`；首次启动会只读加载旧 `settings/koobone.lua` 并复制账号会话、记住密码选项、下载记录和更新偏好。迁移不会修改或删除旧设置文件。
- 继续使用原有 `KOOBONE` 漫画下载目录，避免移动或丢失用户已经下载的 EPUB。
- 增加按屏幕逻辑尺寸和 KOReader DPI 缩放计算的自适应指标；横竖屏切换或屏幕尺寸变化时重新构建网格。
- 设置页增加竖屏列数（2–4）、竖屏行数（1–3）、横屏列数（2–5）、横屏行数（1–3）和标准/大/特大文字选项。
- 放大顶部标题、副标题、搜索/排序/筛选工具栏、书名、作者和底部导航文字，重新计算固定区域高度并缩短书名与作者之间的间距。
- 底部导航图标改为插件自行绘制的黑白几何图形，不再依赖 Kindle 字体中的特殊符号，修复选中“书架”时图标和文字缺失的问题。
- 详情页和账号页改用自适应居中信息面板；下载进度弹窗按横竖屏采用不同宽高比例，并继续显示百分比、字节、速度、预计时间和取消操作。
- 更新器、发布脚本、GitHub Actions、ZIP 顶层目录和 User-Agent 完成新名称适配；发布脚本同时修复相对输出目录无法生成 ZIP 的问题。
- 项目和发布包加入完整 GNU AGPL v3 文本，许可证标识为 `AGPL-3.0-only`。
- 新增旧设置迁移测试和可调网格测试；认证、网格、书架、封面、插件加载、设置迁移、存储、更新策略八组测试通过。
- 已生成并验证 `dist-v0.4.0/release/koocomic.koplugin-v0.4.0.zip`，ZIP 无损检查通过，未包含日志、设置、密码、Cookie、漫画、临时文件或截图。
- v0.4.0 测试包 SHA-256：`64042f4cab7ba8b6cdebd35e9644eef47d737876bec24abdb2a17e128dc54b20`。

安装注意：v0.3.1 与 v0.4.0 的插件目录名不同。真机测试前应删除 Kindle 中旧的 `plugins/koobone.koplugin` 代码目录，再复制新的 `plugins/koocomic.koplugin`；不得删除 `settings/koobone.lua` 或原有漫画下载目录，否则无法迁移旧登录状态或会丢失本地文件。

## 10. 安全与隐私规则

1. 不在源码、文档或提交历史中记录真实邮箱、密码、授权码、Cookie、`sess_key` 或 API Key。
2. 不记录完整的临时 `file_url?sign=...`。
3. 日志默认不输出密码、Cookie 和签名；调试日志必须自动脱敏。
4. 密码默认只停留在登录对话框内存，登录完成后释放。
5. 只有用户主动勾选“记住密码”时才允许本地保存，并明确告知本地明文风险。
6. 退出账号时清除会话和已保存密码。
7. 发布包只包含插件运行所需文件，不包含设置、缓存、日志、漫画或研究资料。
8. 在线更新仅允许 HTTPS 和白名单域名，并验证包大小、SHA-256 和压缩包路径。
9. 公开 GitHub 仓库或 Release 前，必须再次检查提交内容和压缩包是否含敏感信息。
10. 不在文档中直接放置未脱敏截图。

### 截图脱敏标准

如后续确需在 `PROJECT_STATUS.md` 或公开文档中使用截图，必须先生成脱敏副本：

- 邮箱只保留少量首尾字符，其余遮盖。
- 用户名、UID、昵称等可识别信息按需要遮盖。
- 密码、Cookie、Token、签名 URL、二维码和授权码必须完全遮盖。
- 浏览器 Header、Payload、Response 中的敏感值必须遮盖，而不只是裁掉登录框。
- 原图保留在不进入 Git 的本地资料目录；文档只引用脱敏副本。
- 脱敏后应人工放大复查，确认像素不可恢复、文本识别无法读出。

当前 `PROJECT_STATUS.md` 不嵌入任何截图。

## 11. 推荐实施顺序

### 第一阶段：稳定现有闭环

1. 验证不同 KOReader/Kindle 环境中的 HTTPS。
2. 加强 Cookie 多值解析和会话过期识别。
3. 将网络、登录、书库、存储逻辑从 `main.lua` 拆分。
4. 为现有登录、书库和小文件下载补充可复现测试记录。

### 第二阶段：直接改善体验

1. 实时下载进度。
2. `.part` 文件、取消和错误恢复。
3. 登录状态自动恢复。
4. 可选“记住密码”。
5. 账号页和退出账号。
6. 墨水屏友好的书架、详情页和封面缓存。
7. 书库分页和刷新。

### 第三阶段：下载管理和更新

1. 下载任务列表和后台 worker。
2. Range 断点续传。
3. GitHub Release 打包流程。
4. `update.json`、SHA-256、备份安装和回滚。
5. 手动检查更新，再增加可选每日检查。

### 后续阶段

- KOOBONE 与 KOReader 阅读进度映射研究。
- 搜索、标签、收藏和本地文件管理。
- 更新清单签名和更强的启动失败自动回滚。
- 具备足够兼容性与安全审查后再考虑公开发布。

## 12. 下一步

下一步应将 `dist-v0.4.0/release/koocomic.koplugin-v0.4.0.zip` 安装到 Kindle。安装前只删除旧插件代码目录 `plugins/koobone.koplugin`，保留 `settings/koobone.lua` 和漫画下载目录；启动后确认一级菜单出现“koo漫画”、旧账号状态迁移成功，再验证自适应网格、文字对齐、底部书架图标、详情/账号面板、布局设置和下载进度。

真机验证前应备份 Kindle 中现有 `koobone.koplugin` 和 `settings/koobone.lua`。测试日志不得包含密码、Cookie 或完整签名下载地址。v0.4.0 的改名迁移与界面验证通过后，再配置公开 GitHub 仓库和在线更新地址，并决定是否实现 Range 续传和后台 worker。

## 13. 维护日志

### 2026-08-20

已完成：

- 在项目根目录初始化 Git 仓库，默认分支为 `main`。
- 创建 v0.1 首次基线提交，纳入插件源码、文字交接记录和项目状态文档。
- 将 v0.1 原始交接文档中的目标、调研过程、网络流程、实现状态、风险和开发路线整合到 `PROJECT_STATUS.md`。
- 将下载进度、登录记忆、退出账号、书架 UI 和在线更新规划纳入后续路线。
- 明确新版本提醒策略：每天最多弹窗一次，累计三次选择稍后后停止自动提醒，手动检查始终可用。
- 新增 `.gitignore`，避免原始截图、压缩包、日志、临时文件和本地运行数据被误提交。
- 明确截图脱敏和公开发布前的安全检查规则。
- 完成 v0.2.0 第一轮模块化实现，加入下载进度、`.part`、登录恢复、可选记住密码、账号退出、分页书架、详情页、下载管理和设置页面。
- 完成在线更新框架与提醒状态机；因 GitHub 地址尚未配置，当前默认安全关闭在线请求。
- 增加发布脚本、GitHub Release/Pages 工作流和更新提醒状态测试。
- Lua 语法解析、KOReader 接口桩主入口加载、更新策略测试、ZIP 打包、ZIP 完整性、SHA-256、工作流 YAML 与敏感运行文件扫描均通过。
- 按决定不加入安装数、设备型号或匿名遥测统计。
- 收到第一轮 Kindle 真机反馈：插件能够进入登录流程，但 v0.2.0 因静态 `/login.htm` 不下发 `VLIBSID` 而在提交认证前中断。
- 匿名核对当前 KOOBONE 登录入口，确认 `/login.php` 下发 `VLIBSID`，根路径 302 可作为回退；核对输出未包含 Cookie 值。
- 完成 v0.2.1 登录兼容修复和认证 API 回归测试。
- 认证 API、插件加载和更新提醒策略三组测试通过；v0.2.1 ZIP 完整性检查通过。
- 收到第二轮 Kindle 日志，确认书架退出由 `storage.lua` 对 `lfs.attributes` 多返回值处理错误导致，不是内存不足。
- 完成 v0.2.2 `.part` 大小检查、更新包大小检查和书架异常保护修复；存储、书架、认证、插件加载、更新策略五组测试通过。
- 收到真机界面反馈，确认 v0.2.2 仍是临时列表，未达到已规划的三列封面网格。
- 完成 v0.3.0 原生网格书架、渐进封面缓存、搜索、排序、筛选、页码和底部导航编码，并保留列表兼容回退。
- v0.3.0 七组回归测试与 ZIP 完整性检查通过。

未解决：

- v0.3.1 下载网络兼容修复尚待 Kindle/KOReader 真机复测；网格尺寸、字体、触控区域、封面格式兼容和渐进加载体验也仍需验证。
- 服务器端完整书库分页、Range 续传、后台 worker 和下载队列尚未实现。
- 在线更新需要先确定公开 GitHub 仓库与 GitHub Pages 地址，再填写严格的清单 URL 和 Release 前缀。

下一步：

- 安装 v0.3.1 测试 ZIP，先确认 EPUB 下载能开始并持续显示进度；若仍失败，只记录错误文字，不分享 Cookie 或完整签名下载地址。
- 下载通过后按界面稿逐项确认三列书架、工具栏、分页和底部导航，并继续完成封面、账号与设置测试。
- 修复真机兼容问题后，再配置 GitHub 仓库和在线更新地址。

### 2026-08-21

已完成：

- 收到 v0.3.0 Kindle 真机下载失败反馈，错误界面仅显示通用 `network error`。
- 对照 v0.1 已验证下载方式和 KOReader 官方 `socketutil`、OTA 下载实现，确认 HTTPS URL 应通过 `socket.http.request` 的自动 TLS 兼容路径处理。
- 完成 v0.3.1 网络入口和底层错误保留修复，七组回归测试通过。
- 生成并验证 `koobone-koreader-plugin-v0.3.1.zip`；未将 Cookie、密码、签名下载地址、日志或截图写入仓库。
- 确定插件正式名称为“koo漫画”，升级到 v0.4.0，并加入 `AGPL-3.0-only` 完整许可证与非官方项目声明。
- 完成新目录、菜单名称、一级工具入口、旧设置只读迁移、更新器和发布工作流改名。
- 根据两张 Kindle 真机照片修正字号、垂直对齐、书名/作者间距和底部书架缺字；照片只在临时目录查看，没有写入仓库。
- 增加自适应尺寸模块、横竖屏行列设置、三档文字大小、旋转重排、详情/账号信息面板和横竖屏下载弹窗。
- 八组回归测试、Lua 5.1 语法解析、发布打包、ZIP 完整性和敏感运行文件扫描通过。
- 生成 `dist-v0.4.0/release/koocomic.koplugin-v0.4.0.zip`，SHA-256 为 `64042f4cab7ba8b6cdebd35e9644eef47d737876bec24abdb2a17e128dc54b20`。

未解决：

- v0.4.0 需要在实际 Kindle/KOReader 上确认旧设置迁移、一级菜单、自适应网格、字体、触控区域、弹窗尺寸以及 CDN 下载表现。
- Range 断点续传尚未实现，因此保留的 `.part` 目前只用于避免损坏文件冒充完整 EPUB，不用于续传。
- GitHub 公开仓库确定为 `https://github.com/jackchensky/KooComic`；插件展示名仍为“koo漫画”。在线更新清单使用 `https://jackchensky.github.io/KooComic/update.json`，正式启用仍需完成首次 GitHub Release 和 Pages 部署验证。

下一步：

- 备份旧插件与设置，只删除 `plugins/koobone.koplugin` 代码目录，安装 `koocomic.koplugin-v0.4.0.zip`。
- 确认“koo漫画”出现在一级工具菜单、旧账号状态自动迁移、布局设置生效，再复测下载进度和各页面尺寸。
- 若仍有问题，只提供不含密码、Cookie 或完整签名 URL 的错误文字或已脱敏截图。
